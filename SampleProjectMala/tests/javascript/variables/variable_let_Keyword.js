//declare let keyword variable
let cityname="bengaluru"
console.log(cityname);

//declare variables without assigning  value
let dname,loc
dname="Dallas"
loc="Texas"
console.log(dname,loc);

let name
console.log("name is"+name);//undefined

//can we redeclare variable using let keyword :NOOOOO

//var allows to redeclare in same scope but let does not allow to redeclare in same scope
//difference between var and let keyword are 1. let does not allow redeclaration in the same scope
//2. let is block scoped and var is function scoped
//3. let is not hoisted but var is hoisted
//4. let is not attached to window object but var is attached to window object
//5. let is not initialized with undefined but var is initialized with undefined
//6. let is not attached to global object but var is attached to global object

